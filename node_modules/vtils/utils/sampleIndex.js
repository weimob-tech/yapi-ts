import { nthArg } from 'lodash-uni';
import { sampleBy } from "./sampleBy";
/**
 * 从集合中随机获得一个元素的索引（数组）或键（对象）。
 *
 * @param collection 集合
 */

export function sampleIndex(collection) {
  return sampleBy(collection, nthArg(1));
}