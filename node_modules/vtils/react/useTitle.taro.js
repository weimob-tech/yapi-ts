import { setNavigationBarTitle } from '@tarojs/taro';
import { useEffect } from 'react';
export var useTitle = function useTitle(title, // 小程序下获取不到当前页面标题，因此 options.restoreOnUnmount 无效
_options) {
  useEffect(function () {
    setNavigationBarTitle({
      title: title
    });
  }, [title]);
};