"use strict";

exports.__esModule = true;
exports.formatDistanceAgo = formatDistanceAgo;

var _dateFns = require("date-fns");

var _locale = require("date-fns/locale");

/**
 * 将时间转化为 `xxx前` 表示。
 *
 * @param date 时间
 */
function formatDistanceAgo(date) {
  var distance = (0, _dateFns.formatDistanceToNowStrict)(date, {
    locale: _locale.zhCN
  });
  return distance.replace(/\s+/g, '') + "\u524D";
}