"use strict";

exports.__esModule = true;
exports.pascalCase = pascalCase;

var _lodashUni = require("lodash-uni");

/**
 * 转换文本为没有分隔符的大写单词字符串。
 *
 * @param text 要转换的文本
 * @returns 返回结果
 * @example
 * ```typescript
 * pascalCase('test string')
 * // => TestString
 * ```
 */
function pascalCase(text) {
  return (0, _lodashUni.upperFirst)((0, _lodashUni.camelCase)(text));
}