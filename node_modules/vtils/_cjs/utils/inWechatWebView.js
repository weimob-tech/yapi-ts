"use strict";

exports.__esModule = true;
exports.inWechatWebView = inWechatWebView;

var _inBrowser = require("./inBrowser");

var yes;
/**
 * 检查是否在微信内置浏览器中。
 *
 * @returns 返回检查结果
 * @example
 * ```typescript
 * if (inWechatWebView()) {
 *   console.log('你在微信内置浏览器中')
 * }
 * ```
 */

function inWechatWebView() {
  if (yes == null) {
    yes = (0, _inBrowser.inBrowser)() && typeof window.navigator === 'object' && /MicroMessenger/i.test(window.navigator.userAgent || '');
  }

  return yes;
}