"use strict";

exports.__esModule = true;
exports.redirectPageTo = redirectPageTo;

var _navigatePageTo = require("./navigatePageTo");

/**
 * 关闭当前页面，跳转至某个页面，跳转失败时会尝试切换到 Tab 页。
 *
 * @param url 要跳转去的页面地址
 * @param query 查询参数
 */
function redirectPageTo(url, query) {
  return (0, _navigatePageTo.navigatePageTo)(url, query, true);
}