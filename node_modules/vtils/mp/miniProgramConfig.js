import { assign } from "../utils";
var miniProgramConfig = {};
export function getMiniProgramConfig() {
  return miniProgramConfig;
}
export function setMiniProgramConfig(config) {
  assign(miniProgramConfig, config);
  return miniProgramConfig;
}