import { useCallback } from 'react';
import { submit } from "./submit";

/**
 * 对提交类行为的封装。
 */
export function useSubmit(action, deps) {
  return useCallback(function () {
    return submit(action);
  }, deps);
}